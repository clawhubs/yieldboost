# Daily Memory - 2026-04-28

## Project

Hackathon submission hardening for YieldBoost AI with judge mode, wallet-free review flow, mainnet/env audit, and Playwright verification.

## Completed today

- Added `/judge` as a wallet-free review route inside the workspace shell.
- Wired judge mode to live runtime data instead of static placeholders:
  - latest proof / result
  - explorer and contract verification status
  - 0G component inventory
  - Vercel env checklist
  - mainnet cutover checklist
  - demo/public wallet walkthrough
- Added a no-wallet sidebar state with direct actions for:
  - connect wallet
  - demo watch wallet
  - open judge mode
- Promoted `Judge` to the top of the main navigation and made it the primary no-wallet CTA so hackathon judges land on the shortest review path first.
- Cleaned up submission-facing copy issues in the dashboard/agent recommendation flow so apostrophes render correctly instead of leaking as raw HTML entities like `&apos;`.
- Reworked mobile navigation into a left-side drawer triggered by a small Lucide panel icon, then tightened several dashboard cards, popovers, and result blocks so the mobile layout stops overflowing.
- Followed up on the mobile drawer by making the drawer shell and account/CTA cards much more opaque, hiding low-priority social buttons on mobile, and removing the visual overlap where dashboard content bled through the `Connect wallet` area.
- Changed the `Judge` nav entry so a no-wallet reviewer immediately drops into demo watch mode on click instead of needing a second click on the lower CTA.
- Simplified the mobile drawer again so navigation sits in its own solid card and the lower no-wallet panel no longer repeats an `Open judge mode` button.
- Fixed the mobile drawer scroll logic by splitting the drawer into a fixed header plus an explicit internal scroll container so long nav/account content can be scrolled on small screens instead of overlapping.
- Tightened the mobile optimization popup into a scrollable bottom-sheet style dialog and removed fake proof explorer fallback behavior, so proof details now stay honest when no real 0G tx exists.
- Simplified the review flow by turning the visible UX into a single `Judge mode`: the sidebar can now enter and exit judge mode cleanly, optimization is disabled while that mode is active, and `/judge` prefers the latest proof for the active wallet instead of falling back to an unrelated global snapshot.
- Made network-aware server/client surfaces less testnet-hardcoded:
  - wallet RPC config now respects env overrides
  - 0G Compute broker uses the configured server network
  - agent contract routes use active network RPC/explorer config
  - dashboard / stats explorer links follow the active network
- Added a proof-backed portfolio fallback for watch mode when RPC balance is zero but real runtime proof data exists.
- Hardened optimize UX so infra failures do not fake success:
  - optimization still completes
  - proof sync blockers are shown honestly when 0G Storage/indexer fails
- Rewrote Playwright coverage around the real hackathon flows:
  - no-wallet usability
  - connect wallet modal
  - judge page
  - demo watch wallet
  - optimize flow with honest proof blocker handling
  - proof modal / history / agents / docs
- Updated `README.md` with judge mode, env categories, fallback behavior, and mainnet checklist.
- Switched judge-mode data flow to wallet-scoped runtime snapshots:
  - proof records now persist `portfolioSnapshot`
  - `/judge` bootstraps the review wallet even on direct entry
  - dashboard / portfolio / analytics / strategies / watchlist now prefer the latest proof snapshot while judge mode is active
  - exiting judge mode from `/judge` now returns cleanly to `/` instead of re-triggering review bootstrap
- Fixed testnet-vs-mainnet env precedence so testnet flows prefer explicit testnet RPC/storage envs before falling back to legacy shared env names.
- Added contract/tooling paths for the on-chain submission story:
  - `deploy-inft.cjs`
  - network-aware `YIELD_STRATEGY_INFT_MAINNET_ADDRESS` fallback support
  - contract compile test now resolves OpenZeppelin imports correctly
  - broker helper scripts no longer depend on `dotenv` and now honor testnet/mainnet RPC precedence
- Executed live testnet infra steps with the funded builder wallet:
  - deployed `ProofRegistry` to `0x516D005367045b1fc18c9c9a0Ff7bf8653d1B4e3`
  - deployed `YieldStrategyINFT` to `0x9776982bBbf1495cA9AE036775B23d1a2795bb74`
  - completed testnet TEE broker setup:
    - deposit tx `0x7028b3002c2dd849be9266b4821ce7d3fff81bb07df851c63611b26b112be307`
    - transfer tx `0x192f34271e34a9e7f0d6c2101ec0fa0258cf50252e119a0cfb2972f76b3dc634`
- Generated a fresh real testnet proof after the env/network fix:
  - storage tx `0xd086a8015810dfa8cb49242f0c9f2351407ff66d6b95c1eb5586581bdcc073b1`
  - `ProofRegistry` proof id `23`
  - runtime store now has a real `portfolioSnapshot` attached for judge-mode replay
- Fixed a live judge-mode UX regression before the Vercel env sync:
  - mobile/desktop sidebar now keeps `Exit judge mode` visible even while the review wallet is still bootstrapping
  - judge-mode no-wallet card now explains the read-only state and offers an immediate exit path back to the normal user flow
- Synced the production Vercel envs to the fresh testnet judging stack and redeployed:
  - `ZG_NETWORK_KEY=testnet`
  - `ZG_TESTNET_PROOF_REGISTRY_ADDRESS=0x516D005367045b1fc18c9c9a0Ff7bf8653d1B4e3`
  - `YIELD_STRATEGY_INFT_ADDRESS=0x9776982bBbf1495cA9AE036775B23d1a2795bb74`
  - explicit testnet RPC/storage/public explorer envs now point to Galileo instead of relying on ambiguous legacy fallbacks
  - compute signer/provider envs were also pushed so live testnet judging mode matches the funded builder wallet setup
- Cleaned the judge submission surface so the first screen is now juri-first instead of infra-first:
  - removed the top-level runtime/internal status card wall from the primary viewport
  - kept the main screen focused on the latest result, proof receipt, quick routes, and the three primary 0G components
  - moved blockers, mainnet readiness, supporting paths, and env audit behind collapsible sections
  - stopped surfacing the old provider/runtime wording as a primary judge-facing headline

## Verification

- `npm run lint` passed.
- `npm run build` passed.
- `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts tests/docs.spec.ts` passed.
- After judge UI simplification, `npx playwright test tests/audit.spec.ts -g "judge page is reachable without wallet connection"` passed.
- After pushing commit `a4eddad`, `npx playwright test tests/live-smoke.spec.ts` passed against `https://yieldboost-ai.vercel.app`.
- After promoting `Judge` in the sidebar, `npm run lint` and `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts` passed.
- After fixing visible copy typos/entities, `npm run lint` and `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts` passed again.
- After the mobile drawer and responsive dashboard pass, `npm run lint`, `npm run build`, and `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts` passed.
- After pushing commit `5eccd27`, `npx playwright test tests/live-smoke.spec.ts` passed on Vercel and a direct mobile Playwright check confirmed the left drawer opens, avoids horizontal overflow, and reaches `/judge`.
- After the mobile overlap cleanup, `npm run lint` and `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts` passed again, and the updated mobile drawer screenshot no longer shows dashboard copy bleeding through the wallet CTA card.
- After the judge auto-watch change, `npm run lint`, `npm run build`, and `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts` passed, including a dedicated test that `Judge` nav immediately enables watch mode for review.
- After wallet-scoped judge snapshots and direct `/judge` bootstrap, `npm run lint`, `npm run build`, and `npx playwright test tests/dashboard.spec.ts tests/audit.spec.ts` passed again (`13 passed`).
- `node contracts/test-YieldStrategyINFT.js` now compiles successfully with OpenZeppelin imports resolved; full deployment assertions still require a local EVM node.
- `node scripts/deploy-proof-registry.cjs testnet` succeeded.
- `node scripts/deploy-inft.cjs testnet` succeeded.
- `node scripts/setup-tee-broker.cjs testnet` succeeded through deposit, transfer, and provider acknowledgment on testnet.
- `npx playwright test tests/audit.spec.ts -g "1-click optimize writes a real proof from the demo wallet"` passed after the testnet env precedence fix and produced a new real proof record.
- After restoring the always-visible judge exit affordance, `npm run lint`, `npm run build`, and `npx playwright test tests/audit.spec.ts tests/dashboard.spec.ts` passed again (`13 passed`).
- After the Vercel env sync and production redeploy (`dpl_4sW4d15CxSSSbeNjSPcMycLekUyx`), `npx playwright test tests/live-smoke.spec.ts` passed again (`6 passed`) with the live judge page explicitly asserting:
  - `Exit judge mode` is visible
  - `Open latest tx` points to `https://chainscan-galileo.0g.ai/tx/...`
- After the judge page cleanup pass, `npm run lint`, `npm run build`, and `npx playwright test tests/audit.spec.ts tests/dashboard.spec.ts` passed again (`13 passed`).

## Infra findings captured honestly

- The original “wallet has 0 OG / storage fails” diagnosis was partly caused by env precedence drift: testnet routes were seeing mainnet RPC/storage envs first. That precedence is now corrected in code.
- Testnet proof writes are now working again with a fresh runtime proof and `ProofRegistry` entry.
- Testnet TEE broker funding and provider acknowledgment now succeed with the funded wallet.
- The remaining compute blocker is provider connectivity: live TEE inference can still fall back when the remote endpoint resets TLS (`ECONNRESET` to the current provider host), even though broker setup itself is complete.
- Mainnet funding note recorded from the builder for future cutover:
  - `0.01 OG (1 OG)` for deploying `ProofRegistry` and `YieldStrategyINFT` to mainnet
  - `3 OG` for setting up the mainnet TEE broker for Track 2 (`Agentic Trading Arena`)
  - without `3 OG`, contracts can still be deployed, but private execution via the mainnet TEE broker cannot be set up

## Next useful follow-up

- Push the new testnet contract addresses into the actual deployment env that serves judges:
  - `ZG_PROOF_REGISTRY_ADDRESS=0x516D005367045b1fc18c9c9a0Ff7bf8653d1B4e3`
  - `YIELD_STRATEGY_INFT_ADDRESS=0x9776982bBbf1495cA9AE036775B23d1a2795bb74`
- Decide whether Vercel should stay on testnet for judging right now:
  - if yes, ensure `ZG_NETWORK_KEY=testnet` plus the explicit `ZG_TESTNET_*` envs
  - if no, keep mainnet as readiness-only copy and do not market live execution there yet
- Re-test `tests/live-smoke.spec.ts` after pushing and waiting for the latest Vercel deployment.
- Re-test the live TEE endpoint after the broker setup; the remaining failure is transport/provider-side, not funding-side.
- Complete missing mainnet envs and mainnet contract addresses before flipping `ZG_NETWORK_KEY=mainnet`.
